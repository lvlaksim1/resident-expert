# Project identity

Resident Expert is the canonical public framework repository for repository-resident narrow AI experts.

It defines how an expert restores its own durable identity in a fresh chat, consumes another project's Context Capsule as read-only evidence, loads only relevant skills, and returns an independent traceable consultation.

This repository is a framework, not yet a domain-specific authority.
