# Current blockers and open risks

No known framework blocker.

Open design risk: vendor-specific agent adapters can evolve independently, so they must remain adapters and never become the canonical source of expert behavior.
